FoldersMonitorServer
====================

A COM out-of-proc server (services) that monitors the changes in a list of folders on the hard-disk and notifies the subscriber
when a certain event occured.
